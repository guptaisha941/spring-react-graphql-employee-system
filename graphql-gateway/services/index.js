/**
 * Backend service layer.
 * Use Axios here to call downstream REST/GraphQL APIs.
 *
 * Example:
 *   const axios = require('axios');
 *   const client = axios.create({ baseURL: process.env.SERVICE_URL });
 *   module.exports = { client };
 */

module.exports = {};
